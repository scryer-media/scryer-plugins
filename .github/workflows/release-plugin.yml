name: release-plugin

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired release lane
        run: |
          echo "The legacy plugin release lane is retired and frozen."
          echo "Use .github/workflows/release-plugin-v3.yml for current plugin publishing."
          exit 1
